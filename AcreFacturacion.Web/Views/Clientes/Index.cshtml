@model AcreFacturacion.Web.ViewModels.PaginatedList<AcreFacturacion.Web.Models.Cliente>

@{
    ViewData["Title"] = "Clientes";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <div>
        <h1 class="mb-0">Clientes</h1>
        <p class="text-muted">Gestión de clientes registrados.</p>
    </div>

    <a asp-action="Create" class="btn btn-primary">Nuevo cliente</a>
</div>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success">
        @TempData["Success"]
    </div>
}

<form asp-action="Index" method="get" class="row g-2 mb-3">
    <div class="col-md-8">
        <input type="text"
               name="buscar"
               value="@ViewBag.Buscar"
               class="form-control"
               placeholder="Buscar por nombre, identidad/RTN, teléfono o correo" />
    </div>

    <div class="col-md-2 d-grid">
        <button type="submit" class="btn btn-outline-primary">Buscar</button>
    </div>

    <div class="col-md-2 d-grid">
        <a asp-action="Index" class="btn btn-outline-secondary">Limpiar</a>
    </div>
</form>

<div class="card">
    <div class="card-body">
        <table class="table table-striped table-hover align-middle">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Identidad / RTN</th>
                    <th>Teléfono</th>
                    <th>Correo</th>
                    <th>Fecha registro</th>
                    <th>Estado</th>
                    <th class="text-end">Acciones</th>
                </tr>
            </thead>

            <tbody>
                @if (!Model.Items.Any())
                {
                    <tr>
                        <td colspan="7" class="text-center text-muted">
                            No hay clientes registrados.
                        </td>
                    </tr>
                }
                else
                {
                    foreach (var item in Model.Items)
                    {
                        <tr>
                            <td>@item.Nombre</td>
                            <td>@item.IdentidadRTN</td>
                            <td>@item.Telefono</td>
                            <td>@item.Correo</td>
                            <td>@item.FechaRegistro.ToString("dd/MM/yyyy HH:mm")</td>
                            <td>
                                @if (item.Estado)
                                {
                                    <span class="badge bg-success">Activo</span>
                                }
                                else
                                {
                                    <span class="badge bg-secondary">Inactivo</span>
                                }
                            </td>
                            <td class="text-end">
                                <div class="d-inline-flex gap-1">
                                    <a asp-action="Details"
                                       asp-route-id="@item.Id"
                                       class="btn btn-sm btn-outline-info">
                                        Ver
                                    </a>

                                    <a asp-action="Edit"
                                       asp-route-id="@item.Id"
                                       class="btn btn-sm btn-outline-warning">
                                        Editar
                                    </a>

                                    @if (item.Estado)
                                    {
                                        <button type="button"
                                                class="btn btn-sm btn-outline-danger"
                                                data-bs-toggle="modal"
                                                data-bs-target="#estadoClienteModal"
                                                data-url="@Url.Action("Desactivar", "Clientes", new { id = item.Id })"
                                                data-title="Desactivar cliente"
                                                data-message="¿Desea desactivar al cliente @item.Nombre?"
                                                data-button="Desactivar"
                                                data-button-class="btn-danger">
                                            Desactivar
                                        </button>
                                    }
                                    else
                                    {
                                        <button type="button"
                                                class="btn btn-sm btn-outline-success"
                                                data-bs-toggle="modal"
                                                data-bs-target="#estadoClienteModal"
                                                data-url="@Url.Action("Activar", "Clientes", new { id = item.Id })"
                                                data-title="Activar cliente"
                                                data-message="¿Desea activar al cliente @item.Nombre?"
                                                data-button="Activar"
                                                data-button-class="btn-success">
                                            Activar
                                        </button>
                                    }
                                </div>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>

@if (Model.TotalPages > 1)
{
    <nav class="mt-3">
        <ul class="pagination justify-content-end">
            <li class="page-item @(Model.HasPreviousPage ? "" : "disabled")">
                <a class="page-link"
                   asp-action="Index"
                   asp-route-page="@(Model.PageIndex - 1)"
                   asp-route-buscar="@ViewBag.Buscar">
                    Anterior
                </a>
            </li>

            @for (int i = 1; i <= Model.TotalPages; i++)
            {
                <li class="page-item @(i == Model.PageIndex ? "active" : "")">
                    <a class="page-link"
                       asp-action="Index"
                       asp-route-page="@i"
                       asp-route-buscar="@ViewBag.Buscar">
                        @i
                    </a>
                </li>
            }

            <li class="page-item @(Model.HasNextPage ? "" : "disabled")">
                <a class="page-link"
                   asp-action="Index"
                   asp-route-page="@(Model.PageIndex + 1)"
                   asp-route-buscar="@ViewBag.Buscar">
                    Siguiente
                </a>
            </li>
        </ul>
    </nav>
}

<div class="modal fade" id="estadoClienteModal" tabindex="-1" aria-labelledby="estadoClienteModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content app-modal">
            <form id="estadoClienteForm" method="post">
                @Html.AntiForgeryToken()

                <div class="modal-header">
                    <h5 class="modal-title" id="estadoClienteModalLabel">
                        Confirmar acción
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
                </div>

                <div class="modal-body">
                    <p id="estadoClienteMensaje" class="mb-0">
                        ¿Desea continuar?
                    </p>
                </div>

                <div class="modal-footer">
                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">
                        Cancelar
                    </button>

                    <button type="submit" id="estadoClienteBoton" class="btn btn-danger">
                        Confirmar
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        const estadoClienteModal = document.getElementById('estadoClienteModal');

        if (estadoClienteModal) {
            estadoClienteModal.addEventListener('show.bs.modal', function (event) {
                const button = event.relatedTarget;

                const url = button.getAttribute('data-url');
                const title = button.getAttribute('data-title');
                const message = button.getAttribute('data-message');
                const buttonText = button.getAttribute('data-button');
                const buttonClass = button.getAttribute('data-button-class');

                const form = document.getElementById('estadoClienteForm');
                const modalTitle = document.getElementById('estadoClienteModalLabel');
                const modalMessage = document.getElementById('estadoClienteMensaje');
                const confirmButton = document.getElementById('estadoClienteBoton');

                form.setAttribute('action', url);
                modalTitle.textContent = title;
                modalMessage.textContent = message;

                confirmButton.textContent = buttonText;
                confirmButton.className = 'btn ' + buttonClass;
            });
        }
    </script>
}
